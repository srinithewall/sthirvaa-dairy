{
  "AllowMultiple": "false",
  "Description": "\"Revert AI Breeding Service tab to horizontal scrollable comparison table featuring color-coded semen syringe straw images and calf pictures\"",
  "EndLine": "1051",
  "Instruction": "\"Replace card grid with horizontal scrollable comparison table featuring color-coded semen syringe straw images and calf pictures\"",
  "ReplacementContent": "\"            {/* ── Horizontal comparison table ── */}\\n            {!catalogLoading && sortedCatalog.length > 0 && (() => {\\n              const LABEL_W = 90;\\n              const COL_W = 152;\\n              const cols = sortedCatalog;\\n              const bestId = recommendedOrder[0];\\n              const fcPillBg = (pct: number) => pct >= 85 ? '#fce7f3' : pct >= 75 ? '#fef3c7' : pct >= 65 ? '#ede9fe' : '#dbeafe';\\n              const fcPillColor = (pct: number) => pct >= 85 ? '#be185d' : pct >= 75 ? '#92400e' : pct >= 65 ? '#5b21b6' : '#1e40af';\\n              const benefitsList = (s: SemenEntry): string[] =>\\n                Array.isArray(s.benefits) ? s.benefits as string[] : (s.benefits as string).split(',').map(b=>b.trim());\\n\\n              return (\\n                <div className=\\\"overflow-x-auto pb-2\\\" style={{ WebkitOverflowScrolling: 'touch' }}>\\n                  <table\\n                    className=\\\"border-separate\\\"\\n                    style={{\\n                      borderSpacing: 0,\\n                      width: `${LABEL_W + cols.length * COL_W}px`,\\n                      minWidth: `${LABEL_W + Math.min(cols.length, 2) * COL_W}px`,\\n                      maxWidth: '100%',\\n                    }}\\n                  >\\n                    <thead>\\n                      <tr>\\n                        <th style={{ width: LABEL_W, minWidth: LABEL_W }} className=\\\"text-left text-[9px] font-black text-slate-400 uppercase tracking-wider pb-3 pl-4 pr-2 align-bottom\\\">\\n                          Semen<br/>Compare\\n                        </th>\\n                        {cols.map(s => (\\n                          <th key={s.id} style={{ width: COL_W, minWidth: COL_W }} className=\\\"pb-3 px-2 align-bottom\\\">\\n                            {s.id === bestId && (\\n                              <div className=\\\"flex justify-center mb-1.5\\\">\\n                                <span className=\\\"text-[8px] font-black text-white bg-emerald-500 px-2 py-0.5 rounded-full uppercase tracking-wider\\\">⭐ Best Match</\n<truncated 12117 bytes>",
  "StartLine": "888",
  "TargetContent": "\"            {/* ── Semen Card Grid — 2 cols mobile / 4 cols desktop ── */}\\n            {!catalogLoading && sortedCatalog.length > 0 && (()=>{\\n              const bestId = recommendedOrder[0];\\n              const benefitsList = (s: SemenEntry): string[] =>\\n                Array.isArray(s.benefits) ? s.benefits as string[] : (s.benefits as string).split(',').map(b=>b.trim());\\n              const fcPillBg  = (p:number)=>p>=85?'#fce7f3':p>=75?'#fef3c7':p>=65?'#ede9fe':'#dbeafe';\\n              const fcColor   = (p:number)=>p>=85?'#be185d':p>=75?'#92400e':p>=65?'#5b21b6':'#1e40af';\\n\\n              return (\\n                <div className=\\\"px-3 pb-2\\\">\\n                  {/* Section header */}\\n                  <div className=\\\"flex items-center justify-between mb-3 px-0.5\\\">\\n                    <div>\\n                      <p className=\\\"text-[13px] font-black text-slate-800\\\">Choose Semen for {herd.animalName}</p>\\n                      <p className=\\\"text-[10px] text-slate-400 mt-0.5\\\">{sortedCatalog.length} options • tap card to compare</p>\\n                    </div>\\n                    <span className=\\\"text-[9px] font-black text-emerald-700 bg-emerald-100 px-2.5 py-1 rounded-full flex-shrink-0\\\">\\n                      ⭐ {sortedCatalog[0]?.breedName}\\n                    </span>\\n                  </div>\\n\\n                  {/* Card grid */}\\n                  <div className=\\\"grid grid-cols-2 lg:grid-cols-4 gap-2.5\\\">\\n                    {sortedCatalog.map(s => {\\n                      const isBest = s.id === bestId;\\n                      const bens = benefitsList(s);\\n                      return (\\n                        <div key={s.id}\\n                          className={`relative rounded-2xl overflow-hidden flex flex-col bg-white transition-all active:scale-[0.98] ${\\n                            isBest\\n                              ? 'shadow-lg shadow-emerald-100 ring-2 ring-emerald-500'\\n                              : 'shadow-md ring-1 ring-slate-200'\\n                     \n<truncated 8760 bytes>",
  "TargetFile": "\"c:/SpringBoot/WorkSpace/dairy/dairy-ui/components/AnimalWorkspace.tsx\"",
  "toolAction": "\"Replace card grid with horizontal scrollable comparison table\"",
  "toolSummary": "\"Replace card grid with horizontal scrollable comparison table\""
}