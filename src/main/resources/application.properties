spring.application.name=DairyManagementSystem
server.port=8080

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://43.204.221.192:3306/dairy?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USER:sthi}
spring.datasource.password=${DB_PASSWORD:Sthirva@26}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=5

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT and Security
jwt.secret=${JWT_SECRET:DairySecretKeyForJwtAuthenticationWhichShouldBeLongEnoughToAvoidErrors}
app.jwtSecret=${jwt.secret}
jwt.expirationMs=86400000
app.jwtExpirationMs=${jwt.expirationMs}

# Proxy / Forwarded Headers support
server.forward-headers-strategy=framework

# Logging
logging.level.com.dairy=DEBUG
logging.level.org.springframework.security=INFO

# OpenAPI (optional)
# springdoc.api-docs.path=/api-docs

# Cloudflare R2 Configurations
r2.endpoint=${R2_ENDPOINT:https://ab568b8e1adc53437e5e4f4e0cf1867b.r2.cloudflarestorage.com}
r2.access-key=${R2_ACCESS_KEY:cf2bff36204659316198d232eb158cd7}
r2.secret-key=${R2_SECRET_KEY:4e1927e13c704ea7e902c838c57caf914f86b1f6d5502aa81f1c189208fe8390}
r2.bucket=${R2_BUCKET:sthirvaa-assets}
r2.public-url=${R2_PUBLIC_URL:https://pub-2ec91de366c4458e88df0bcd70bf560d.r2.dev}
